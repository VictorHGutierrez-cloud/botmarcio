{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "export npm_config_cache=/tmp/.npm-cache && export PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true && npm ci --prefer-offline --no-audit --legacy-peer-deps"
  },
  "deploy": {
    "startCommand": "npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

